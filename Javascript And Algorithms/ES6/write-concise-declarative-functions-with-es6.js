// Only change code below this line
const bicycle = {
  
  setGear(newGear) {
    this.gear= newGear;
    return this.gear;
  }
};
// Only change code above this line
bicycle.setGear(3);
console.log(bicycle.gear);